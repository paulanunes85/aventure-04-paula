# checkpoints/: Sala do Eco

Mapas legíveis por máquina: `spec-to-plan.yaml` (/plan-architecture), `plan-to-tasks.yaml` e `test-coverage.yaml` (/break-down-tasks).

| Arquivo | Finalidade | Data |
| --- | --- | --- |
| NÃO APLICÁVEL: nenhum arquivo ainda; a etapa dona não foi executada | - | - |
